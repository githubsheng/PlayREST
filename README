#Installation

Assuming you have `sbt` installed.

1. git clone the project
2. navigate to the project folder, and begin a sbt session by typing `sbt`
3. wait for a long time
4. type in `run 9000`, the server will start running on `localhost:9000`
5. wait for a long time
